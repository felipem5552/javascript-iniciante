// 📌 Atividade: Rotina simples (Array)

// Neste exercício, vamos aprender a usar **arrays** em JavaScript.
// Arrays são listas que guardam vários valores dentro de uma mesma variável.
// Cada item da lista tem um índice (posição), que começa do número 0.

// 1. Crie um array chamado "rotina" que guarda apenas textos (strings)
// Cada elemento é uma atividade do dia.
var rotina = ["acordar", "escovar os dentes", "tomar café da manhã", "estudar"]

// 2. Mostre cada item da rotina separadamente.
// Para isso, usamos o índice de cada elemento do array.
console.log(rotina[0]) // Resultado esperado: acordar
console.log(rotina[1]) // Resultado esperado: escovar os dentes
console.log(rotina[2]) // Resultado esperado: tomar café da manhã
console.log(rotina[3]) // Resultado esperado: estudar

// 3. Observações importantes:
// - Arrays em JavaScript começam na posição 0 (primeiro elemento).
// - Para acessar um item: nomeDoArray[indice]
// - Aqui usamos números de 0 a 3, pois o array tem 4 elementos.
